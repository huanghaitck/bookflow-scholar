# Manual/web-assisted review

Use only the supplied source pages. Preserve stable IDs, source hashes, raw values and units. Mark uncertainty explicitly; do not guess. Return `manual_patch.json`. Web-assisted review is not Provider validation.
